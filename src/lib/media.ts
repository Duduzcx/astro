/** Footage lives in /public/videos. One place to rename it. */
export const media = {
  /** Blue particle wave, 10s, 1080p — the hero field. */
  particleWave: '/videos/12280043_1920_1080_30fps.mp4',
  /** Neon circuit flyover, 30s, 4K — the full-bleed band. */
  circuitFlight: '/videos/15439973-uhd_3840_2160_30fps.mp4',
} as const
