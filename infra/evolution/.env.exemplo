# Copie para .env e preencha. Nenhum destes valores vai para o repositório.
SERVER_URL=https://whatsapp.seudominio.com.br
DOMINIO=whatsapp.seudominio.com.br
# Gere com: openssl rand -hex 32  — é a EVOLUTION_API_KEY da Vercel.
AUTHENTICATION_API_KEY=
# Gere com: openssl rand -hex 16
POSTGRES_PASSWORD=
